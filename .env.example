# This project uses EdgeOne Functions for backend API.
# COZE_TOKEN, COZE_API, and COZE_WORKFLOW_ID should be configured
# as environment variables in the EdgeOne console (not here).

# Vite build-time env vars (only if needed):
# (none currently required)
